<?php
use Carbon_Fields\Container;
use Carbon_Fields\Field;

$basic_options_container_header = Container::make('theme_options', 'Footer')
    ->set_icon('dashicons-table-row-after')
    ->add_fields([

    ]);